https://codepen.io/chris-burton/pen/abmRLEM?editors=1100



1. What is Semantic HTML? Tags that gives an some information about what is contained within.

2. What is HTML used for? Website structure

3. What is an attribute and where do we put it? Additional information placed within the start tag.

4. What is the h1 tag used for? How many times should I use it on a page? Most important heading. Only one should be used per page.

5. Name two tags that have required attributes - <img> <a>

6. What do we put in the head of our HTML document? meta data, stylesheet links, website title, scripts, links(ex. awesomefont link)

7. What is an id? a tag selector that should only be used once per page

8. What elements can I add an id to? any element can have an id

9. How many times can I use the same id on a page? You can use it as many times as you want but it should only be used once.

10. What is a class? a selector (.classname)

11. What elements can I add a class to? Any

12. How many times can I use the same class on a page? As much as you want.

13. How do I get my link to open in a new tab? add target="_blank"

14. What is the alt attribute in the image tag used for? SEO and screen readers

15. How do I reference an id? #id-name

16. What is the difference between a section and a div? <section> groups blocks of related content <div> is generic grouping

17. What is CSS used for? Styling websites

18. How to we select an element? Example - every h2 on the page?  h2 {}

19. What is the difference between a class and an id? - Give me an example of when I might use each one. Classes can be used to select multiple elements. Ids should only be used once per page to select that one specific element. A class can be used on multiple elements to apply common styling. 

20. How do we select classes in CSS? .classname {}

21. How do we select a p element with a single class of “human””? p.human {}

22. What is a parent child selector? When would this be useful? It selects all of the elements that are a direct child of the parent.

23. How do you select all links within a div with the class of sidebar?  .sidebar a {}

24. What is a pseudo selector? It is used to select the state of an element (:hover, :link, :visited, etc)

25. What do we use the change the spacing between lines? line-height: *;

26. What do we use to change the spacing between letters? letter-spacing: *;

27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform: uppercase; text-transform: lowercase; text-transform: capitalize;

28. How do I add a 1px border around my div that is dotted and black? border: 1px dotted #000;

29. How do I select everything on the page? * {}

30. How do I write a comment in CSS?   // Comment

31. How do I find out what file I am in, when I am using the command line? look at the prompt

32. Using the command line - how do I see a list of files/folders in my current folder? ls

33. How do I remove a file via the command line? Why do I have to be careful with this? rm file.name. There is no prompt asking if you're sure. Once it's removed, you can't get it back.

34. Why should I use version control? To keep track of changes made.

35. How often should I commit to github? Very often. 

36. What is the command we would use to push our repo up to github? git push

37. Walk me through Lambda's git flow. 
  Fork repo
  Clone repo to local using git clone "web link" 
  cd into the cloned repo 
  Branch using git checkout -b 'firstname-lastname'
  Work on repo
  git add .
  git commit -m "Descriptive Message"
  git push -u origin firstname-lastname
  Submit Pull Request

Stretch Questions

    1. What is the difference between an inline element and a block element? Block elements start on a new line. inline elements do not start on a new line.

    2. What happens when an element is positioned absolutely? It is positioned relative to the closest position ancestor.

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? margin: auto *px;
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
       Block: <header> <nav> <footer> 
       Inline: <a> <button> <span>
       Inline-block: <img>

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them?
      The box model holds the margin, border, and padding around an element. 
      The "fix" is to use box-sizing: border-box; 